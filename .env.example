PORT=14785
#BASE_DIR="./" # server will not leave this directory (eg ../../..), default process.cwd()
#INTERNAL_VDB="./serverDB" # Valthera DB server system database path
#PARSERS_DIR="./parsers" # custom parsers directory

#SSL_CERT="./certs/cert.pem" # enable HTTPS by setting path to SSL certificate
#SSL_KEY="./certs/key.pem" # SSL private key (required if SSL_CERT is set)
#SSL_CA="./certs/ca.pem" # optional CA bundle
#SSL_PORT=14786 # HTTPS port (default: HTTP port + 1)
#HTTP_ENABLED=false # set to "false" to disable HTTP (HTTPS-only mode)
#KEEPALIVE_TIMEOUT=60000 # ms, how long to keep idle connections open (default: 60000)

#RATE_LIMIT_ONCE_MAX=5 # max requests per window for once routes (login, getDbList, auth-check)
#RATE_LIMIT_ONCE_WINDOW=60000 # ms, once routes rate limit window (default: 60s)
#RATE_LIMIT_API_MAX=100 # max API requests per window
#RATE_LIMIT_API_WINDOW=60000 # ms, API rate limit window (default: 60s)

# Logging
#LOG_LEVEL=INFO # ERROR, WARN, INFO, DEBUG
#LOG_FILE=./logs/server.log # path to server log file

# Audit
#AUDIT_ENABLED=true # enable/disable audit logging
#AUDIT_LOG_FILE=./logs/audit.log # path to audit log file
#AUDIT_LOG_AUTH=true # log authentication events (login, token validation)
#AUDIT_LOG_READ=false # log read operations (find, findOne, getCollections)
#AUDIT_LOG_WRITE=true # log write operations (add, update, remove)
#AUDIT_LOG_PERMISSION=true # log permission changes
#AUDIT_LOG_ADMIN=true # log admin operations
#AUDIT_INCLUDE_PAYLOAD=false # include request body in audit logs
#AUDIT_INCLUDE_IP=true # include client IP in audit logs
#AUDIT_INCLUDE_RESULT=true # include operation result in audit logs
